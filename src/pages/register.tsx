import React from 'react';
import RegisterComp from '../auth/Register';

const Register = () => {
    return (
        <div>
            <RegisterComp />
        </div>
    );
};

export default Register;
